// Single source of truth for the Sample Review row model = the data lib.
// v2 | 2026-06-09 | Job_PM (CPO) -- reconciled to re-export from lib/admin/sample-review.
export type {
  SampleCommsItem,
  SampleExternalProfile,
  SampleReviewStatus,
  SampleReviewRow,
} from '@/lib/admin/sample-review';
